language: python
sudo: true
python:
  - "2.7"

branches:
  only:
    - master
    - develop

# commands to install dependencies
install:
    - pip install Cython
    - pip install -r requirements.txt

# command to run tests
script: nosetests