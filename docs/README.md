# Documentation

The documentation sources live in this folder. The rendered site is **not** committed: it is built by the `documentation` GitHub workflow on every push and published to <https://matthiaskoenig.github.io/sbmlutils> from `develop`.

How to build the site locally, how the API reference is generated and how to add a page is described in [Development](development.md#documentation).
