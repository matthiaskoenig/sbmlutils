% model: {{ model.getId() }}
% Autogenerated ODE System from SBML with sbmlutils (https://github.com/matthiaskoenig/sbmlutils.git).
%
% add the following packages:
% \usepackage{amsmath}
% \usepackage{breqn}
The units of the ODE system are\\
\tiny
\begin{align*}
&time \quad \text{[{{units["time"]}}]}\\
&substance \quad \text{[{{units["substance"]}}]}\\
&extent \quad \text{[{{units["extent"]}}]}\\
&volume \quad \text{[{{units["volume"]}}]}\\
&area \quad \text{[{{units["area"]}}]}\\
&length \quad \text{[{{units["length"]}}]}
\end{align*}
\normalsize
The parameters are\\
\tiny
\begin{align*}
{% for id in pids %}
{{ id.replace("_", "\_") }} &= {{p[id]}} \quad \text{[{{p_units[id]}}] {{names[id]}}}\\
{% endfor %}
\end{align*}
\normalsize
The rules and reactions are\\
\tiny
\begin{dgroup*}
{% for id in yids %}
\begin{dmath*}[compact]
{{ id.replace("_", "\_") }} = {{y[id].replace("_", "\_").replace(" * ", " \cdot ")}}\quad \text{[{{y_units[id]}}] {{names[id]}}}
\end{dmath*}
{% endfor %}
\end{dgroup*}
\normalsize
The resulting ODE system is\\
\tiny
\begin{align*}
{% for id in xids %}
\frac{d {{ id.replace("_", "\_") }}}{dt} &= {{ dx[id] }}\\
{% endfor %}
\end{align*}
\normalsize
