[tox]
envlist = ty, py3.{11,12,13,14}

[testenv]
package = wheel
wheel_build_env = .pkg
deps =
    pytest
commands =
    pytest

[testenv:ty]
passenv =
    TY_OUTPUT_FORMAT
deps =
    ty
    pytest
commands =
    ty check
